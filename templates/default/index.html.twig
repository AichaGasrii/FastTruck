{% extends 'base1.html.twig' %}

{% block body %}

<div class="example-wrapper">
    <h1> QR-Code ✅</h1> <hr>

    <div class="row">
        <div class="col-md-6">
            {{ form_start(form) }}
                {{ form_row(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Recherche'}}) }}
                <hr>
                <button type="submit" class="btn btn-success btn-block">Recherche</button>
            {{ form_end(form) }}
        </div>
        <div class="col-md-6">
            {% if qrCode %}
                <a href="{{ path('commande_index') }}"><img src="{{ qrCode }}" alt="qr-code"></a>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
