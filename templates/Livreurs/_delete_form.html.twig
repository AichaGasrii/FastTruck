<form method="post" action="{{ path('livreurs_delete', {'id': livreurs.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livreurs.id) }}">
    <button class="btn">Delete</button>
</form>
