{% extends 'base.html.twig' %}

{% block title %}Offre de travail{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ offre.prenom }}</td>
            </tr>
            <tr>
                <th>num</th>
                <td>{{ offre.num }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ offre.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_index') }}">back to list</a>

    <a href="{{ path('offre_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
